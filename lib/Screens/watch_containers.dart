import 'package:flutter/material.dart';

class WatchContainers extends StatelessWidget {
  const WatchContainers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Ver mis contenedores'),
    );
  }
}
