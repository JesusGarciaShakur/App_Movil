import 'package:flutter/material.dart';

class WaterPump extends StatelessWidget {
  const WaterPump({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Programar bomba'),
    );
  }
}
